(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const data={strategy:['FIND THE SIGNAL','Positioning, architecture and a clear interaction model before pixels start moving.','RESEARCH / SITEMAP / DIRECTION / PROTOTYPE','084'],design:['SHAPE THE SURFACE','A visual language built for clarity, distinction and interaction people remember.','ART DIRECTION / UI / IDENTITY / PROTOTYPE','091'],motion:['MAKE IT RESPOND','Motion becomes part of the interface logic: transitions, feedback and pacing that make the surface feel alive.','MOTION SYSTEM / GSAP / 3D / FEEDBACK','097'],build:['MAKE IT REAL','Production-grade front-end engineering with performance, accessibility and experimental interaction kept intact.','HTML / CSS / JS / WEBGL / OPTIMIZATION','088']};
const nodes=$$('.home-service-node');function set(k){const d=data[k];nodes.forEach(n=>n.classList.toggle('active',n.dataset.service===k));$('#home-service-code').textContent='SERVICE // '+k.toUpperCase();$('#home-service-title').textContent=d[0];$('#home-service-desc').textContent=d[1];$('#home-service-output').textContent=d[2];$('#home-service-signal').textContent=d[3];document.documentElement.dataset.homeService=k}nodes.forEach(n=>n.addEventListener('click',()=>set(n.dataset.service)));if(nodes.length)set('strategy');
const canvas=$('#home-service-canvas');if(canvas){const ctx=canvas.getContext('2d');let w=0,h=0,dpr=1,visible=true,raf=0;const resize=()=>{const r=canvas.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.5);canvas.width=Math.max(1,r.width*dpr);canvas.height=Math.max(1,r.height*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);w=r.width;h=r.height};resize();addEventListener('resize',resize,{passive:true});new IntersectionObserver(([e])=>{visible=e.isIntersecting;if(visible&&!raf)raf=requestAnimationFrame(draw)},{threshold:0}).observe(canvas);function draw(t){raf=0;if(!visible)return;ctx.clearRect(0,0,w,h);const cx=w/2,cy=h/2,R=Math.min(w,h)*.36;for(let i=0;i<5;i++){const r=R*(.35+i*.15),a=t*.0002*(i+1)+i;ctx.beginPath();ctx.ellipse(cx,cy,r,r*.42,Math.sin(t*.0001)*.08,0,Math.PI*2);ctx.strokeStyle=`rgba(255,255,255,${.035+i*.012})`;ctx.stroke();const x=cx+Math.cos(a)*r,y=cy+Math.sin(a)*r*.42;ctx.fillStyle='#aaa';ctx.beginPath();ctx.arc(x,y,2+i*.3,0,Math.PI*2);ctx.fill()}raf=requestAnimationFrame(draw)}raf=requestAnimationFrame(draw)}

/* COMMAND HEADER MACHINERY */
const header=$('.site-header');
if(header){
 const nav=$('#main-nav'), toggle=$('#menu-toggle');
 const links=$$('.nav-link',nav); const clock=$('.header-clock .clock');
 const route=location.pathname.split('/').pop().replace('.html','').toLowerCase()||'index';
 links.forEach(a=>{const href=(a.getAttribute('href')||'').split('/').pop().split('#')[0].replace('.html','').toLowerCase()||'index';a.classList.toggle('active',href===route);a.dataset.routeId=String(links.indexOf(a)+1).padStart(2,'0')+' / '+href.toUpperCase();a.setAttribute('aria-label',a.textContent.trim()+' — '+a.dataset.routeId)});
 links.forEach(a=>a.addEventListener('pointerenter',()=>{header.dataset.signal=a.dataset.routeId||'ROUTE';document.documentElement.style.setProperty('--header-signal',JSON.stringify(a.dataset.routeId||'ROUTE')),{passive:true}));
 if(toggle&&nav){toggle.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');toggle.setAttribute('aria-expanded',String(open));nav.setAttribute('aria-hidden',String(!open))});document.addEventListener('click',e=>{if(!header.contains(e.target))header.classList.remove('menu-open')},{passive:true});links.forEach(a=>a.addEventListener('click',()=>header.classList.remove('menu-open'),{passive:true}))}
 const tick=()=>{if(clock){const d=new Date(),z=n=>String(n).padStart(2,'0');clock.textContent=`${z(d.getHours())}:${z(d.getMinutes())}:${z(d.getSeconds())}`}requestAnimationFrame(tick)};tick();
 const updateScroll=()=>{const y=scrollY;header.classList.toggle('header-scrolled',y>24);document.documentElement.style.setProperty('--header-scroll',Math.min(1,y/400).toFixed(3))};addEventListener('scroll',updateScroll,{passive:true});updateScroll();
}
})();
