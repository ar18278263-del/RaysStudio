(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const data={
 strategy:{title:'FIND THE SIGNAL',desc:'Positioning, architecture and a clear interaction model before pixels start moving.',output:'Research / Sitemap / Direction / Prototype',tools:['DISCOVERY','ARCHITECTURE','DECISION SYSTEM'],signal:84,focus:'FOUNDATION'},
 design:{title:'SHAPE THE SURFACE',desc:'A visual language built for clarity, distinction and the kind of interaction people remember.',output:'Art Direction / UI / Identity / Prototype',tools:['VISUAL SYSTEM','INTERACTION','ART DIRECTION'],signal:91,focus:'LANGUAGE'},
 motion:{title:'MAKE IT RESPOND',desc:'Motion becomes part of the interface logic: transitions, feedback, pacing and the little details that make a surface feel alive.',output:'Motion System / GSAP / 3D / Feedback',tools:['TIMING','TRANSITIONS','RESPONSE'],signal:97,focus:'BEHAVIOUR'},
 build:{title:'MAKE IT REAL',desc:'Production-grade front-end engineering with performance, accessibility and experimental interaction kept intact.',output:'HTML / CSS / JS / WebGL / Optimization',tools:['FRONTEND','WEBGL','DEPLOYMENT'],signal:88,focus:'DELIVERY'}
};
const tabs=$$('.svc-tab'), code=$('#svc-code'), sig=$('#svc-signal'), title=$('#svc-title'), desc=$('#svc-desc'), output=$('#svc-output'), state=$('#svc-state'), mode=$('#svc-mode'), load=$('#svc-load');
function setService(key){const d=data[key];if(!d)return;tabs.forEach(t=>{const on=t.dataset.service===key;t.classList.toggle('active',on);t.setAttribute('aria-selected',String(on))});code.textContent='SERVICE // '+key.toUpperCase();sig.textContent='SIGNAL '+String(d.signal).padStart(3,'0');title.textContent=d.title;desc.textContent=d.desc;output.textContent=d.output;$('#svc-tool-a').textContent=d.tools[0];$('#svc-tool-b').textContent=d.tools[1];$('#svc-tool-c').textContent=d.tools[2];state.textContent='LIVE';mode.textContent=d.focus;load.textContent=Math.round(18+d.signal%23)+'%';document.documentElement.dataset.service=key;$('#orbit-readout').textContent=String(Object.keys(data).indexOf(key)+1).padStart(2,'0')+' / '+key.toUpperCase()}
tabs.forEach((t,i)=>t.addEventListener('click',()=>setService(t.dataset.service)));setService('strategy');
const checks=$$('.svc-option input'), index=$('#build-index'), bar=$('#build-bar'), complexity=$('#complexity'), focus=$('#focus'), copy=$('#config-copy');
function config(){const active=checks.filter(c=>c.checked).map(c=>c.value),n=active.length;index.textContent=String(n).padStart(2,'0');bar.style.width=(n/4*100)+'%';const map={0:['EMPTY','RESET THE FIELD','Start with a signal and the system will define the next move.'],1:['LIGHT','SINGLE STATE','One focused capability. Ideal for a contained problem or early prototype.'],2:['MEDIUM','FOUNDATION','A focused system: clear strategy plus a sharp visual language, ready to move into production.'],3:['HIGH','RESPONSIVE','A multi-layer build with motion or engineering depth switched on.'],4:['MAXIMUM','FULL SYSTEM','Strategy, design, motion and engineering working as one continuous surface.']};const m=map[n];complexity.textContent=m[0];focus.textContent=m[1];copy.textContent=m[2];$$('.svc-option').forEach(o=>o.classList.toggle('active',o.querySelector('input').checked))}
checks.forEach(c=>c.addEventListener('change',config));config();
const canvas=$('#service-orbit');
if(canvas){const ctx=canvas.getContext('2d');let w=0,h=0,dpr=1,raf=0,visible=true;const resize=()=>{const r=canvas.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,1.5);canvas.width=Math.max(1,r.width*dpr);canvas.height=Math.max(1,r.height*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);w=r.width;h=r.height};resize();addEventListener('resize',resize,{passive:true});new IntersectionObserver(([e])=>{visible=e.isIntersecting;if(visible&&!raf)raf=requestAnimationFrame(draw)},{threshold:0}).observe(canvas);const draw=t=>{raf=0;if(!visible)return;ctx.clearRect(0,0,w,h);const cx=w/2,cy=h/2,scale=Math.min(w,h)/2.4;for(let i=0;i<4;i++){const r=scale*(.25+i*.18);ctx.beginPath();ctx.ellipse(cx,cy,r,r*.42,Math.sin(t*.00015+i)*.08,0,Math.PI*2);ctx.strokeStyle=`rgba(255,255,255,${.05+i*.014})`;ctx.stroke();const a=t*.00025*(i+1)+i*1.7,x=cx+Math.cos(a)*r,y=cy+Math.sin(a)*r*.42;ctx.fillStyle='#aaa';ctx.beginPath();ctx.arc(x,y,2.5+i*.4,0,Math.PI*2);ctx.fill()}for(let i=0;i<18;i++){const a=i/18*Math.PI*2+t*.00005;const r=scale*(.8+Math.sin(i*4.2)*.08);ctx.fillStyle='rgba(255,255,255,.08)';ctx.fillRect(cx+Math.cos(a)*r,cy+Math.sin(a)*r*.55,1.5,1.5)}raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw)}
})();
